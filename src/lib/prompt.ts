import { stories as builtinStories } from '../stories'
import { storyGlosses } from './parse'
import { getCustomStories, getFeedback, getWords } from './storage'
import type { Feedback } from './types'

const FEEDBACK_RU: Record<Feedback, string> = {
  easy: 'легко',
  ok: 'нормально',
  hard: 'сложно',
}

function calibration(values: Feedback[]): string {
  if (values.length === 0) return 'Оценок пока нет — держи стандартный уровень A1.'
  const recent = values.slice(-3)
  const hard = recent.filter((v) => v === 'hard').length
  const easy = recent.filter((v) => v === 'easy').length
  if (hard >= 2)
    return 'Последние истории были СЛОЖНЫМИ. Сделай текст заметно проще: короткие предложения (5–8 слов), только Präsens, самая базовая лексика, больше глосс (12–15).'
  if (hard >= 1)
    return 'Была сложная история. Держи уровень чуть проще стандартного A1: простые короткие предложения, только Präsens.'
  if (easy === recent.length)
    return 'Все последние истории были ЛЁГКИМИ. Сделай текст чуть сложнее: предложения длиннее, можно немного Perfekt и модальные глаголы, меньше глосс (6–8).'
  return 'Уровень подобран хорошо — держи такой же.'
}

/** The reader's vocabulary, shared by the generate and import prompts. */
function vocabulary() {
  const feedback = getFeedback()
  const words = getWords()
  const stories = [...builtinStories, ...getCustomStories()]

  const readStories = stories.filter((s) => feedback[s.id])

  const learnedWords = words.filter((w) => w.learned).map((w) => w.word)
  const learningWords = words.filter((w) => !w.learned).map((w) => `${w.word} (${w.gloss})`)

  const knownGlosses = new Set<string>()
  for (const s of readStories) {
    for (const g of storyGlosses(s)) {
      if (!words.some((w) => w.word.toLowerCase() === g.word.toLowerCase()))
        knownGlosses.add(g.word)
    }
  }

  return { feedback, stories, readStories, learnedWords, learningWords, knownGlosses }
}

const MARKUP_RULES = `- Выдели сложные для A1 слова или ВЫРАЖЕНИЯ маркером {{Wort|перевод}} — перевод на русский, коротко, в той же грамматической форме. Глоссируй осмысленные единицы целиком, как в книгах для изучающих: {{läuft dunkelrot an|багровеет}}, {{zu Hause vergessen|забыл дома}}. Каждую единицу глоссируй только при первом появлении.
- Если единица разорвана в предложении (отделяемая приставка, Perfekt), пометь позднюю часть маркером {{+...}}: «Er {{steht|встаёт (aufstehen)}} früh {{+auf}}.» — перевод пиши только у первой части и указывай в нём инфинитив. ВАЖНО: между частями разорванной пары не ставь других маркеров {{...}} — часть {{+...}} привязывается к ближайшей предыдущей глоссе.
- Добавь поле "dict": объект «слово → перевод» для ВСЕХ остальных слов текста (ключи в нижнем регистре, без имён собственных). Переводи по роли слова в этом тексте: «die Bank» в парке — «скамейка». Части составных форм объясняй с отсылкой к целому: для «hat … gekauft» → "hat": "вспомогательный глагол (hat gekauft = купила)", "gekauft": "купила (Perfekt от kaufen)".
- Добавь поле "nouns": объект «существительное → артикль» (der/die/das) для существительных текста. Ключ — форма слова, как она встречается в тексте, в нижнем регистре; для множественного числа артикль "die"; без имён собственных. Напр.: {"decke": "die", "himmel": "der", "kinder": "die"}.
- Добавь поле "cover": обложка-иллюстрация к сюжету — простой плоский SVG одной строкой, viewBox="0 0 640 360", крупные геометрические формы, 4–6 цветов из тёплой палитры (#faf7f2 фон, #d9534f акцент, #2c2825, приглушённые зелёный/голубой/жёлтый), без текста и без <script>. В атрибутах SVG используй одинарные кавычки.
- Добавь поле "quiz": РОВНО 3 вопроса на понимание НА НЕМЕЦКОМ. Формат каждого: { "q": "вопрос", "options": ["A","B","C"], "answer": <индекс правильного, с 0> }. Качество (важно): (1) проверяй понимание СЮЖЕТА — детали, причину/следствие, что произошло в конце и почему; не бери первое предложение и не делай вопрос, на который можно ответить не читая; (2) все 3 варианта правдоподобны и взяты из деталей ЭТОЙ истории (другой персонаж/предмет/место, которые упоминались) — никаких абсурдных или посторонних; (3) запрещены да/нет и варианты Ja/Nein/Vielleicht; (4) варианты одной длины и вида, правильный не выделяется и не является дословной цитатой (переформулируй); (5) три вопроса — про РАЗНЫЕ части истории (начало / середина / финал). Плохо: «Bekommt sie den Job? — Ja/Nein/Vielleicht». Хорошо: «Warum ruft Mia am Ende Tom an? — Sie hat den Job bekommen / Sie kommt zu spät / Sie sucht ein anderes Café».`

const JSON_FORMAT = `Ответь ТОЛЬКО валидным JSON в этом формате (без markdown-обёртки):
{
  "id": "kebab-case-id",
  "title": "Заголовок на немецком",
  "titleRu": "Перевод заголовка",
  "level": "A1",
  "paragraphs": ["Абзац 1 с {{маркерами|глоссами}}...", "Абзац 2..."],
  "dict": {"слово": "перевод в контексте", "...": "..."},
  "nouns": {"decke": "die", "himmel": "der", "...": "der/die/das"},
  "cover": "<svg viewBox='0 0 640 360' xmlns='http://www.w3.org/2000/svg'>...</svg>",
  "quiz": [{"q": "Frage auf Deutsch?", "options": ["A", "B", "C"], "answer": 0}]
}`

function vocabularySections(v: ReturnType<typeof vocabulary>): string {
  return `${
    v.learningWords.length
      ? `\nЭти слова я сейчас учу — обязательно глоссируй их, если они встречаются:\n${v.learningWords.join(', ')}`
      : ''
  }
${
  v.learnedWords.length
    ? `\nЭти слова я уже знаю твёрдо — не глоссируй их (но включай в dict):\n${v.learnedWords.join(', ')}`
    : ''
}
${
  v.knownGlosses.size
    ? `\nЭти слова мне уже встречались и не показались сложными — можешь оставить их БЕЗ глосс:\n${[...v.knownGlosses].join(', ')}`
    : ''
}`
}

export function buildPrompt(): string {
  const { feedback, stories, readStories, learnedWords, learningWords, knownGlosses } =
    vocabulary()

  const feedbackLines = readStories.map(
    (s) => `- «${s.title}» — ${FEEDBACK_RU[feedback[s.id]]}`,
  )

  const existingTitles = stories.map((s) => `«${s.title}»`).join(', ')

  return `Ты помогаешь мне учить немецкий (уровень A1, родной язык — русский). Напиши новую короткую историю для чтения.

Требования к истории:
- Уровень A1–A2: простая лексика, простые предложения, в основном Präsens (немного Perfekt — можно).
- Длина: 180–250 слов, 5–7 абзацев (3–5 минут чтения).
- Живой сюжет из повседневной жизни, с маленьким поворотом или юмором в конце.
- Не повторяй темы уже существующих историй: ${existingTitles}.
- Выдели 8–12 сложных для A1 слов или ВЫРАЖЕНИЙ маркером {{Wort|перевод}} — перевод на русский, коротко, в той же грамматической форме. Глоссируй осмысленные единицы целиком, как в книгах для изучающих: {{läuft dunkelrot an|багровеет}}, {{zu Hause vergessen|забыл дома}}. Каждую единицу глоссируй только при первом появлении.
- Если единица разорвана в предложении (отделяемая приставка, Perfekt), пометь позднюю часть маркером {{+...}}: «Er {{steht|встаёт (aufstehen)}} früh {{+auf}}.» — перевод пиши только у первой части и указывай в нём инфинитив. ВАЖНО: между частями разорванной пары не ставь других маркеров {{...}} — часть {{+...}} привязывается к ближайшей предыдущей глоссе.
- Добавь поле "dict": объект «слово → перевод» для ВСЕХ остальных слов текста (ключи в нижнем регистре, без имён собственных). Переводи по роли слова в этом тексте: «die Bank» в парке — «скамейка». Части составных форм объясняй с отсылкой к целому: для «hat … gekauft» → "hat": "вспомогательный глагол (hat gekauft = купила)", "gekauft": "купила (Perfekt от kaufen)".
- Добавь поле "nouns": объект «существительное → артикль» (der/die/das) для существительных текста. Ключ — форма слова, как она встречается в тексте, в нижнем регистре; для множественного числа артикль "die"; без имён собственных. Напр.: {"decke": "die", "himmel": "der", "kinder": "die"}.
- Добавь поле "cover": обложка-иллюстрация к сюжету — простой плоский SVG одной строкой, viewBox="0 0 640 360", крупные геометрические формы, 4–6 цветов из тёплой палитры (#faf7f2 фон, #d9534f акцент, #2c2825, приглушённые зелёный/голубой/жёлтый), без текста и без <script>. В атрибутах SVG используй одинарные кавычки.
- Добавь поле "quiz": РОВНО 3 вопроса на понимание НА НЕМЕЦКОМ. Формат каждого: { "q": "вопрос", "options": ["A","B","C"], "answer": <индекс правильного, с 0> }. Качество (важно): (1) проверяй понимание СЮЖЕТА — детали, причину/следствие, что произошло в конце и почему; не бери первое предложение и не делай вопрос, на который можно ответить не читая; (2) все 3 варианта правдоподобны и взяты из деталей ЭТОЙ истории (другой персонаж/предмет/место, которые упоминались) — никаких абсурдных или посторонних; (3) запрещены да/нет и варианты Ja/Nein/Vielleicht; (4) варианты одной длины и вида, правильный не выделяется и не является дословной цитатой (переформулируй); (5) три вопроса — про РАЗНЫЕ части истории (начало / середина / финал). Плохо: «Bekommt sie den Job? — Ja/Nein/Vielleicht». Хорошо: «Warum ruft Mia am Ende Tom an? — Sie hat den Job bekommen / Sie kommt zu spät / Sie sucht ein anderes Café».

Калибровка сложности по моим оценкам прочитанного:
${calibration(readStories.map((s) => feedback[s.id]))}
${feedbackLines.length ? `\nМои оценки историй:\n${feedbackLines.join('\n')}` : ''}
${
  learningWords.length
    ? `\nЭти слова я сейчас учу — постарайся естественно использовать 2–4 из них в тексте и глоссируй их:\n${learningWords.join(', ')}`
    : ''
}
${
  learnedWords.length
    ? `\nЭти слова я уже знаю твёрдо — смело используй их без глосс, они закрепляют выученное:\n${learnedWords.join(', ')}`
    : ''
}
${
  knownGlosses.size
    ? `\nЭти слова мне уже встречались и не показались сложными — можешь использовать их БЕЗ глосс:\n${[...knownGlosses].join(', ')}`
    : ''
}

Ответь ТОЛЬКО валидным JSON в этом формате (без markdown-обёртки):
{
  "id": "kebab-case-id",
  "title": "Заголовок на немецком",
  "titleRu": "Перевод заголовка",
  "level": "A1",
  "paragraphs": ["Абзац 1 с {{маркерами|глоссами}}...", "Абзац 2..."],
  "dict": {"слово": "перевод в контексте", "...": "..."},
  "nouns": {"decke": "die", "himmel": "der", "...": "der/die/das"},
  "cover": "<svg viewBox='0 0 640 360' xmlns='http://www.w3.org/2000/svg'>...</svg>",
  "quiz": [{"q": "Frage auf Deutsch?", "options": ["A", "B", "C"], "answer": 0}]
}

Полученный JSON я вставлю в приложение на экране «Добавить историю».`
}

/**
 * Prompt that marks up a ready-made German text the reader found elsewhere:
 * the text is kept verbatim, Claude only adds glosses, the dict, and metadata.
 */
export function buildImportPrompt(sourceText: string): string {
  const v = vocabulary()

  return `Ты помогаешь мне учить немецкий (родной язык — русский). Я нашла готовый немецкий текст и хочу читать его в своём приложении. Подготовь его.

Требования:
- НЕ переписывай и НЕ упрощай текст — сохрани его дословно, включая пунктуацию. Можно только разбить его на абзацы (4–8 предложений в абзаце), если исходное разбиение потерялось при копировании.
- Определи уровень текста (A1–C1) и запиши его в поле "level".
- Придумай короткий заголовок на немецком (если в тексте нет своего) и его перевод "titleRu".
${MARKUP_RULES}
- Глоссируй слова и выражения, которые могут быть сложными для моего уровня — ориентируйся на списки моих слов ниже. Не больше ~15 глосс на историю: выбирай самые полезные, остальное уйдёт в dict.
${vocabularySections(v)}

${JSON_FORMAT}

Вот текст:

"""
${sourceText.trim()}
"""

Полученный JSON я вставлю в приложение на экране «Добавить историю».`
}
